import type { DeepOptional } from '@pui/core';

/**
 * Placeholder type for component configs
 * This will be extended when components are imported
 */
export type Components = Record<string, unknown>;
