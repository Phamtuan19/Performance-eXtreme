import { Distance } from './distance.type';

const distance: Distance = {
   width: {
      keys: 'width',
      transform: 'spacing',
   },
   minWidth: {
      keys: 'minWidth',
      transform: 'spacing',
   },
   maxWidth: {
      keys: 'maxWidth',
      transform: 'spacing',
   },
   height: {
      keys: 'height',
      transform: 'spacing',
   },
   minHeight: {
      keys: 'minHeight',
      transform: 'spacing',
   },
   maxHeight: {
      keys: 'maxHeight',
      transform: 'spacing',
   },
   top: {},
   right: {},
   bottom: {},
   left: {},
};

export default distance;
